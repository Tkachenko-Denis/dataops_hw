# hw_19

## Files

- `Dockerfile`
- `docker-compose.yaml`
- `.env`

## Services

- `db` on `${POSTGRES_PORT}`
- `minio` on `${MINIO_API_PORT}` and console `${MINIO_CONSOLE_PORT}`
- `lakefs` on `${LAKEFS_PORT}`
